import React from 'react';
import SliderDuenoAlquila from '../../../molecules/sliders/shared/sliderDuenoAlquila';

const style = { position: 'fixed', height: '100%' };
const IntroDuenoAlquila = () => (
  <section id="slide-intro" style={style}>
    <SliderDuenoAlquila />
  </section>

);

export default IntroDuenoAlquila;

