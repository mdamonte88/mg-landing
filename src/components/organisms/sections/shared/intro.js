import React from 'react';

const style = { position: 'fixed', height: '100%' };
const Intro = () => (
  <section id="slide-intro" style={style}>
    <span> “En un mundo tan cambiante,<br /> las empresas necesitan soluciones<br /> a prueba de futuro.”  </span>
  </section>
);

export default Intro;

