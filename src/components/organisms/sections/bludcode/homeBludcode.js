import React from 'react';
import SliderBludcode from '../../../molecules/sliders/bludcode/sliderBludcode';

const homeStyle = { height: 'auto' };
const HomeBludcoe = () => (
  <section className="homeSlide contentbox section-home-hero" style={homeStyle}>
    <SliderBludcode />
  </section>
);

export default HomeBludcoe;
